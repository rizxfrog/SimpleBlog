server:
  servlet:
    encoding:
      charset: utf-8
      enabled: true
      force: true
  port: 8888

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/simple_blog
    username: user_RYTz54
    password: password_amC7Wx
  data:
    redis:
      host: localhost
      port: 6379
      password: redis_fQHPzE
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME:rabbitmq}
    password: ${RABBITMQ_PASSWORD:rabbitmq}
  elasticsearch:
    uris: ${ELASTICSEARCH_URIS:http://localhost:9200}
    username: elastic
    password: Elastic_McTtDX
  graphql:
    graphiql:
      enabled: true
    schema:
      locations: classpath:graphql/
  jackson:
    time-zone: Asia/Shanghai
  mail:
    host: ${SMTP_HOST:smtp.sina.com}
    port: ${SMTP_PORT:587}  #  465 SSL; 587 STARTTLS
    username: ${SMTP_USERNAME:autumn2567@sina.com}
    password: ${SMTP_PASSWORD:3d83fdb2de53c8f2}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  servlet:
    multipart:
      max-file-size: 200MB
      max-request-size: 200MB

mybatis-plus:
  type-handlers-package: com.simpleblog.mapper.typehandler
  configuration:
    map-underscore-to-camel-case: true

app:
  jwt:
    secret: o0WUqpArX0G6QGVeE9buf6fzwrJvWSuIEYvNkdBmxgj3bK0M/NX/DYU8P8M60OAz7wP1UyBBqoiHMsKmZstLdQ==
    expiration-seconds: 86400
  admin:
    enabled: true
    username: admin
    password: admin123
    display-name: Admin
  storage:
    endpoint: ${RUSTFS_ENDPOINT:http://localhost:9000}
    access-key: ${RUSTFS_ACCESS_KEY:rustfsadmin}
    secret-key: ${RUSTFS_SECRET_KEY:rustfsadmin_YFSt4t}
    bucket: ${RUSTFS_BUCKET:simpleblog}
    region: ${RUSTFS_REGION:us-east-1}
    public-url: ${RUSTFS_PUBLIC_URL:http://localhost:9000/simpleblog}
    path-style-access: ${RUSTFS_PATH_STYLE:true}
